substitutions:
  name: esphome-web-81f8e8
  friendly_name: Group-Alarm-Bot
  task_stack_in_psram: "true"

  # ===== Board-Specific Pinouts =====
  # I2S audio pins
  i2s_lrclk_pin: GPIO15
  i2s_bclk_pin: GPIO14
  i2s_dout_pin: GPIO16

  # TAS5805M
  tas58xx_variant: TAS5805M
  tas58xx_enable_pin: GPIO17
  i2c_sda_pin: GPIO8
  i2c_scl_pin: GPIO9

  # ===== DAC Overrides (optional, package defaults shown) =====
  tas58xx_dac_mode: PBTL 
  tas58xx_mixer_mode: MONO
  tas58xx_analog_gain: 0db
  tas58xx_volume_max: 0dB
  tas58xx_volume_min: -60dB
  tas58xx_preset_default_index: 3

  # RGB LED
  rgb_led_pin: GPIO40
  num_leds: 12

  # SPI pins (for display)
  spi_clk_pin: GPIO12
  spi_mosi_pin: GPIO11
  spi_miso_pin: GPIO13
  
  # ===== Snapcast Server Configuration =====
  snapcast_server_ip: "192.168.1.49"
  snapcast_server_port: "1704"

  
esphome:
  name: ${name}
  friendly_name: ${friendly_name}
  min_version: 2026.1.4
  name_add_mac_suffix: false
  platformio_options:
    board_build.flash_mode: dio

esp32:
  variant: ESP32S3
  flash_size: 8MB
  cpu_frequency: 240MHz
  framework:
    type: esp-idf
    version: recommended
    sdkconfig_options:
      CONFIG_ESP32S3_DATA_CACHE_64KB: "y"
      CONFIG_ESP32S3_DATA_CACHE_LINE_64B: "y"
      CONFIG_ESP32S3_INSTRUCTION_CACHE_32KB: "y"
      # Moves instructions and read only data from flash into PSRAM on boot.
      # Both enabled allows instructions to execute while a flash operation is in progress without needing to be placed in IRAM.
      # Considerably speeds up mWW at the cost of using more PSRAM.
      CONFIG_SPIRAM_RODATA: "y"
      CONFIG_SPIRAM_FETCH_INSTRUCTIONS: "y"
      CONFIG_BT_ALLOCATION_FROM_SPIRAM_FIRST: "y"
      CONFIG_BT_BLE_DYNAMIC_ENV_MEMORY: "y"
      CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC: "y"
      CONFIG_MBEDTLS_SSL_PROTO_TLS1_3: "y" 

logger:
  level: DEBUG

api:

ota:
  platform: esphome
  password: !secret esphome_ota_password

wifi:
  ssid: !secret esphome_wifi_ssid
  password: !secret esphome_wifi_password

psram:
  mode: octal
  speed: 80MHz
  ignore_not_found: false

network:
  enable_ipv6: true

# ===== Packages =====
packages:
  # esparagus_remote:
  #   url: https://github.com/sonocotta/esparagus-media-center
  #   ref: main
  #   files:
  #     - firmware/esphome/packages/dac-tas58xx-presets.yaml
  #     - firmware/esphome/packages/snapclient-newer.yaml
  #     - firmware/esphome/packages/snapclient-addon-dac-enable.yaml
  #     - firmware/esphome/packages/snapclient-addon-tas58xx-presets.yaml
  #     - firmware/esphome/packages/monitoring.yaml
  #     - firmware/esphome/packages/monitoring-wifi.yaml
  #     - firmware/esphome/packages/oled-i2c.yaml
  #     - firmware/esphome/packages/light-x.yaml
  #     # - firmware/esphome/packages/ethernet-w5500.yaml


  # Debug/local development fallback:
  # external_dac: !include packages/dac-tas58xx.yaml
  # external_dac: !include packages/dac-tas58xx-biamp.yaml
  external_dac: !include packages/dac-tas58xx-presets.yaml
  monitoring: !include packages/monitoring.yaml
  monitoring_wifi: !include packages/monitoring-wifi.yaml
  oled: !include packages/oled-i2c.yaml
  light: !include packages/light-x.yaml
  snapclient: !include packages/snapclient-newer.yaml
  snapclient_dac_enabler: !include packages/snapclient-addon-dac-enable.yaml
  snapclient_tas58xx_enabler: !include packages/snapclient-addon-tas58xx-presets.yaml
  # ethernet: !include packages/ethernet-w5500.yaml
