# ============================================================
# Esparagus-Echo-Solo - Sendspin Configuration
# ============================================================
# ESP32-S3 audio board with single MAX98357A DAC and onboard I2S microphone
# Configured with Sendspin for synchronized multi-room audio
# Board-specific pinouts defined in substitutions
# Functional blocks imported from /packages
#
# Ported from the sibling esparagus-echo repo (hardware rev B -
# firmware/esphome/echo-solo-b/echo-solo-b-idf-sendspin.yaml there,
# the only revision that exists for Echo Solo) so this device's
# factory/OTA pipeline can live alongside every other board's, in
# this repo. Keep in sync by hand if the source changes.
# ============================================================

substitutions:
  name: echo-solo
  friendly_name: Esparagus-Echo-Solo
  task_stack_in_psram: "true"

  # ===== Board-Specific Pinouts =====
  # I2S audio pins
  i2s_lrclk_pin: GPIO8
  i2s_bclk_pin: GPIO18
  i2s_dout_pin: GPIO17

  # I2S microphone pins
  i2s_mic_lrclk_pin: GPIO7
  i2s_mic_bclk_pin: GPIO16
  i2s_mic_din_pin: GPIO15

  # DAC enable pins
  dac_enable_pin: GPIO9
  dac_enable_restore_mode: ALWAYS_OFF

  # RGB LED
  rgb_led_pin: GPIO42
  num_leds: 2

esphome:
  name: ${name}
  friendly_name: ${friendly_name}
  min_version: 2026.5.0
  name_add_mac_suffix: false
  platformio_options:
    board_build.flash_mode: dio

esp32:
  variant: ESP32S3
  flash_size: 8MB
  cpu_frequency: 240MHz
  framework:
    type: esp-idf
    version: recommended
    sdkconfig_options:
      CONFIG_ESP32S3_DATA_CACHE_64KB: "y"
      CONFIG_ESP32S3_DATA_CACHE_LINE_64B: "y"
      CONFIG_ESP32S3_INSTRUCTION_CACHE_32KB: "y"
      # Moves instructions and read only data from flash into PSRAM on boot.
      # Both enabled allows instructions to execute while a flash operation is in progress without needing to be placed in IRAM.
      # Considerably speeds up mWW at the cost of using more PSRAM.
      CONFIG_SPIRAM_RODATA: "y"
      CONFIG_SPIRAM_FETCH_INSTRUCTIONS: "y"
      CONFIG_BT_ALLOCATION_FROM_SPIRAM_FIRST: "y"
      CONFIG_BT_BLE_DYNAMIC_ENV_MEMORY: "y"
      CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC: "y"
      CONFIG_MBEDTLS_SSL_PROTO_TLS1_3: "y"

logger:
  level: DEBUG

api:

ota:
  platform: esphome
  password: !secret esphome_ota_password

wifi:
  ssid: !secret esphome_wifi_ssid
  password: !secret esphome_wifi_password

psram:
  mode: octal
  speed: 80MHz
  ignore_not_found: false

network:
  enable_ipv6: true

# ===== Packages =====
packages:
  esparagus_remote:
    url: https://github.com/sonocotta/esparagus-media-center
    ref: main
    files:
      - firmware/esphome/packages/audio.yaml
      - firmware/esphome/packages/sendspin.yaml
      - firmware/esphome/packages/sendspin-addon-dac-enable.yaml

      - firmware/esphome/packages/dac-switch.yaml
      - firmware/esphome/packages/light-x.yaml
      - firmware/esphome/packages/monitoring.yaml
      - firmware/esphome/packages/monitoring-wifi.yaml

  # Debug/local development fallback:
  # audio: !include packages/audio.yaml
  # sendspin: !include packages/sendspin.yaml
  # sendspin_addon_dac_enable: !include packages/sendspin-addon-dac-enable.yaml
  # dac_switch: !include packages/dac-switch.yaml
  # light: !include packages/light-x.yaml
  # monitoring: !include packages/monitoring.yaml
  # monitoring_wifi: !include packages/monitoring-wifi.yaml
